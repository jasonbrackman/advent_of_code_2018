// use regex::Regex;
// use std::collections::{HashMap, HashSet};

pub fn doit(input: &str) {
    for line in input.lines() {
        println!("{}", line);
    }
}

#[test]
fn test_doit() {
    assert_eq!(0, 0);
}